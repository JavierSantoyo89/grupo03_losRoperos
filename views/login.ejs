<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cabin&family=Outfit:wght@100&family=Roboto:wght@100&display=swap" rel="stylesheet">   
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>        
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@100&family=Roboto:wght@100&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
    <link rel="stylesheet" href="../css/styles.css" type="text/css" media="all">
    <link rel="stylesheet" href="../css/cssLogin.css" type="text/css" media="all">
    <title>Login</title>
</head>
    <body class="login_Body">
    <%-include ("./partials/header.ejs")%>
    <%-include ("./partials/banner.ejs")%>

            <% if(typeof errors != 'undefined'){ %>
                <p style="color: red">Error, animal!!!.</p>
                <ul>
                    <% for (let i =0; i < errors.length; i++){ %>
                        <li>
                            <% errors[i].msg%>
                        </li>
                    <%}%>
                </ul>
            <% } %>


            <form class="login_Formulario" method="POST" action="/user/login">
                <h1>Login</h1> 
                <% if (locals && locals.errors) {%>
                    <% errors.forEach(error => { %> 
                        <p class="feedback is-danger"> <%= error.msg %> </p>
                    <% }) %>
                <% } %> 
                <div class="contenedor_Login">
                   
                        <div class="input_contenedor_Login">
                            <i class="fas fa-envelope icon_Login"></i>
                            <input type="email" placeholder="Correo Electronico" name ='email'>    
                        </div>
                            <div class="input_contenedor_Login">
                                <i class="fas fa-key icon_Login"></i>
                                <input type="password" placeholder="Contraseña" name="password">    
                            </div>
                        </div>
                        <div class="input_contenedor_Login">
                            <i class="fas fa-key icon_Login"></i>
                             <label> Recordarme: </label>
                             <input type="checkbox" name="Chk_recordarme">    
                        </div>
                       
                    <input type="submit" value="Ingresar" class="button_Login">    
        
            <p class="texto1">Al registrarte, aceptas nuestras Condiciones de uso y Politica de privacidad.</p>
            <p class="texto1">¿Si no, tienes una cuenta? <a class="link_Login" href="/user/register">!Registrate¡</a></p>
                    </div>
            </form>
    <%-include ("./partials/footer.ejs")%>
    </body>
</html>